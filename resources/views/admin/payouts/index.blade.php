@extends('layouts.dashboard')
@section('title','Payout Administration')
@section('content')
<div class="dashboard-hero"><div><div class="dashboard-kicker">Finance Operations</div><h1 class="dashboard-title">Seller Payouts</h1><p class="dashboard-subtitle">Approve, complete or reverse seller withdrawal requests with an auditable ledger.</p></div></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<section class="dashboard-panel"><div class="table-wrap"><table class="data-table"><thead><tr><th>Seller</th><th>Amount</th><th>Method</th><th>Phone</th><th>Status</th><th>Reference</th><th>Actions</th></tr></thead><tbody>@forelse($payouts as $payout)<tr><td>{{ $payout->seller->name }}</td><td>UGX {{ number_format($payout->amount,0) }}</td><td>{{ strtoupper($payout->provider) }}</td><td>{{ $payout->phone }}</td><td>{{ ucfirst($payout->status) }}</td><td>{{ $payout->merchant_reference }}</td><td>@if($payout->status === 'pending')<form method="POST" action="{{ route('admin.payouts.approve',$payout) }}">@csrf @method('PATCH')<button class="btn btn-primary" type="submit">Approve</button></form>@elseif(in_array($payout->status,['approved','processing']))<form method="POST" action="{{ route('admin.payouts.paid',$payout) }}">@csrf @method('PATCH')<input name="provider_reference" placeholder="Provider reference" required><button class="btn btn-primary" type="submit">Mark paid</button></form><form method="POST" action="{{ route('admin.payouts.fail',$payout) }}">@csrf @method('PATCH')<input name="reason" placeholder="Failure reason" required><button class="btn" type="submit">Fail</button></form>@else — @endif</td></tr>@empty<tr><td colspan="7">No payout requests yet.</td></tr>@endforelse</tbody></table></div>{{ $payouts->links() }}</section>
@endsection
